from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='ML project to predict the trip duration of each rider nyc',
    author='Noel Soares',
    license='',
)
